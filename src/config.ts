/** Andrea Tino - 2020 */

/**
 * Output format.
 * This is the format that the user can ask to the engine.
 * Pandoc will generate an output which might not strictly be
 * the same value.
 */
export type OutputFormat = "pdf" | "html";
